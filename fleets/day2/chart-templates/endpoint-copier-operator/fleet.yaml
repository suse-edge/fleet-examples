defaultNamespace: endpoint-copier

helm:
  releaseName: endpoint-copier-operator
  chart: "oci://registry.suse.com/edge/3.1/endpoint-copier-operator-chart"
  version: "0.2.1"
  # custom chart value overrides
  values: {}
